# Through the load balancer, for normal traffic
IDEM_API_URL=http://localhost:8080
# Direct to each instance, for the health strip and targeted sends
API_1_URL=http://localhost:8081
API_2_URL=http://localhost:8082
# Read-only, so the lab can show you the actual rows
DATABASE_URL=postgres://idem:idem@localhost:5432/idem
